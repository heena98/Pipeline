pipeline {
    agent { label 'maven'}

    stages {
        stage('clone') {
            steps {
               git branch: 'main', url: 'https://github.com/heena98/Pipeline.git'            }
        }
    }
}
